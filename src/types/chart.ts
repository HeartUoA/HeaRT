import { Dimension } from "./dimension";

export type Chart = {
  id: string;
  courseID: string;
  createdAt: Date;
  dimensions: Dimension[];
};

export const createChart = (chart: any) => {
  return;
};
